#include <stdio.h>

int fact(int n){
 	int factorial;
 if (n == 1||n==0){
 	return 1;
}
 else {
 	return n*fact(n-1);
 }
 }

int main(){
	int n;
	printf("\nEnter a number n: ");
	scanf("%d",&n);
 	printf("%d",fact(n));
 return 0;
 }
